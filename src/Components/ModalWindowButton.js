import React, { useState } from "react";
import PostForm from "./JSONForm.js";

import { Link } from "react-router-dom";

import {
  Button,
  TextField,
  Dialog,
  DialogActions,
  DialogContent,
  DialogContentText,
  DialogTitle,
  Grid,
} from "@material-ui/core";

/*
ModalWindowButton is a component that opens a modal window when clicked

props

buttonText:string - button text
windowTitle:string - title of the window
windowText:JSX - text of modal window appears before the body
windowBody:JSX - content of the modal window- forms and buttons

*/
function ModalWindowButton(props) {}

export default ModalWindowButton;
